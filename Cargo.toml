[package]
name = "aria_poc"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
claude-agent = "0.2"
futures = "0.3"
tokio = { version = "1", features = ["io-util", "net", "rt-multi-thread", "macros", "sync", "time"] }
serde_json = "1"
